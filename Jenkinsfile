pipeline {
    agent any
    stages {
        stage('Checkout Test') {
            steps {
                sh 'echo "Jenkins connected to repo successfully"'
                sh 'ls -la'
            }
        }
    }
}